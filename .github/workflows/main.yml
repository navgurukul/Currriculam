name: Deploy to Production

on:
  push:
    branches:
      - Dev 
  pull_request:
    branches:
      - Dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Check for Conflicts
        run: git diff --quiet origin/Dev || { echo "There are conflicts in the code."; exit 1; }
      - name: Install Dependencies
        run: npm install
      - name: Build Production Code
        run: npm run build
      - name: Deploy to Production
        # uses: easingthemes/ssh-deploy@main
        # with:
        #   server: ${{ secrets.PRODUCTION_SERVER }}
        #   username: ${{ secrets.PRODUCTION_USERNAME }}
        #   password: ${{ secrets.PRODUCTION_PASSWORD }}
        #   local_path: ./Curriculum
        # remote_path: /var/www/html
        # env:
        #   SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        #   # ARGS: "-rlgoDzvc -i --delete"
        #   ARGS: "-rlgoDzvc -i --delete --exclude-from=.rsync.rules --exclude=/Currriculam/ --exclude=/node_modules/"

        #   SOURCE: "public/"
        #   REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
        #   REMOTE_USER: ${{ secrets.REMOTE_USER }}
        #   TARGET: ${{ secrets.REMOTE_TARGET }}
        #   # EXCLUDE: "/Currriculam/, /node_modules/"
        uses: easingthemes/ssh-deploy@v2.3.10
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          ssh-known-hosts: ${{ secrets.REMOTE_HOST }}
          remote-user: ${{ secrets.REMOTE_USER }}
          server-ip: ${{ secrets.PRODUCTION_PASSWORD }}
          local-path: ./Currriculam
          remote-path: /var/www/html

